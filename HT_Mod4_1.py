from pathlib import Path

def total_salary(path):
   with open(path, 'r') as fh:
      print(f" fh = {fh}")

path = Path("salary_file.txt")
with open(path, 'r') as fh:
      fh.
      print(f" fh = {fh}")

total_salary(path)
